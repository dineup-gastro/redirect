services:
  {{REF_NAME}}-redirect:
    image: ghcr.io/{{REPOSITORY}}:{{SHA}}
    environment:
      - PORT=80
    labels:
      - reprox.host={{DOMAIN}}
    networks:
      - reprox_network
networks:
  reprox_network:
    external: true
